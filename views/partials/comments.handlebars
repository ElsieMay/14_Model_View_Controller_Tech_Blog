<div class="comments">
	{{#each this}}
		<section class="comment">
			<div class="meta">
				{{username}}
				on
				{{format_date_long created_at}}
			</div>
			<div class="text">
				{{comment_text}}
			</div>
		</section>
	{{/each}}
</div>